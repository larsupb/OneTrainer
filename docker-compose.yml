services:
    rest-server:
        build:
            context: .
            dockerfile: Dockerfile
        image: onetrainer/rest-server:latest
        ports:
            - "8000:8000"
        deploy:
            resources:
                reservations:
                    devices:
                        -   driver: nvidia
                            count: all
                            capabilities: [ gpu ]
